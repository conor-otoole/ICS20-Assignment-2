/* Created by: Conor
* Created on: Oct 2022
* This file contains the JavaScript for index.html Assignment#1
*/
/* Function */
function calculate() {
  /* Input */
  const base = parseInt(document.getElementById("parallelogramBase").value)
    const base = parseInt(document.getElementById("parallelogramHeight").value)

  /* Process */
  const area = base * height

  /* Output */
  document.getElementById(*area*). innerHTML = "The area is: " + area + " cm²"
}